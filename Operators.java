public class Operators {
    public static void main (String [] args){
    int a = 10 , b=3;
    int sum = a + b;
    int diff = a - b;
    int mul = a * b;
    int div = a/b;
    int mod= a % b;
    //print output


    //Unary operators - need only one operand | used to increment decrement or negate a value.
    int num_1 = 10;
    int num_2 = 10;
    // a++ -> 10 ; ++a -> 12; b-- -> 10 --b -> 8 

    //Assignment operator - assigns value to a variable on left hand side (right to left associativity)
   int num=10;
   num+=3;
   // *= , -= , /= , %= 
    }
    //Arithmetic operators - arithmetic operations on simple non primitive or primitive datatypes


    // Relational Operators - they return boolean after comparison and are extinsively used in 
    // looping statements as well as if else

        int a = 1, b = 3;

        //>, < , >= , <= , == , != 

        //  Logical Operators - perform AND and OR 

        // Ternary operators - short hand version of if else statements
        // ?:

        //Bitwise operators 
        // manipulate bits using AND OR NOT XOR
        // left shift and right shift - multiplying and dividing by powers of two.

        //  & |  ^ ~ << >> >>>


        // instance of operator - used for type checking check if object is instance of class
        //  , or a subclass , or interface
}

